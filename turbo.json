{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"],
      "cache": false,
      "outputMode": "full"
    }
  },
  "globalDependencies": ["tsconfig.json"]
}
